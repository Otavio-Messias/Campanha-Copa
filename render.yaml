services:
  - type: web
    name: campanha-copa
    env: python
    runtime: python3
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: DASHBOARD_PASSWORD
        value: copa2024
      - key: RENDER
        value: "true"
